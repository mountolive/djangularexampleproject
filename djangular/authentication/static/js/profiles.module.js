(function () {
  'use strict';

  angular
    .module('mountolive.profiles', [
      'mountolive.profiles.controllers',
      'mountolive.profiles.services'
    ]);

  angular
    .module('mountolive.profiles.controllers', []);

  angular
    .module('mountolive.profiles.services', []);
})();